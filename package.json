{
  "name": "virtualtopdc",
  "version": "1.0.0",
  "private": true,
  "description": "virtualTOP$DC — OdiLeague live-sync RNG analytics and simulation bot",
  "proxy": "http://localhost:4000",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "server": "node server/index.js",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run server\" \"npm start\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
